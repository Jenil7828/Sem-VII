# Requirements for the Streamlit colorizer app
streamlit>=1.34.0
opencv-python-headless>=4.8.0
numpy>=1.24.0
pillow>=10.0
pandas>=2.0.0

# Note: the `torch` entry below will install the CPU build by default on many
# platforms. To use CUDA-enabled PyTorch (GPU), install PyTorch in your venv
# manually using one of the recommended commands instead of embedding wheel
# index flags into this file.
#
# Conda (recommended on Windows):
#   conda install pytorch torchvision pytorch-cuda=12.9 -c pytorch -c nvidia
#
# Pip example (CUDA 12.9) — run manually in your venv:
#   python -m pip install --upgrade pip
#   python -m pip install --extra-index-url https://download.pytorch.org/whl/cu129 \
#       torch==2.2.0+cu129 torchvision==0.17.1+cu129
#
# If you do not need GPU, leave `torch` as-is to install the CPU build.
torch>=2.2.0
